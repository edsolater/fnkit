import { Fraction } from "./types"

export const TenBigint = BigInt(10)
export const OneBigint = BigInt(1)
export const ZeroBigint = BigInt(0)

export const fractionZero = { numerator: 0n, denominator: 1n } as Fraction
export const fractionOne = { numerator: 1n, denominator: 1n } as Fraction
